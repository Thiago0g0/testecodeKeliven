const{ addUser, gerUser, deleteUser} = require('../src/users')
const{addOrder, getOrder, deleteOrder} = require('../src/orders')

describe('Teste de integracao entre usuarios e pedidos', () =>{
    
})